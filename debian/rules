#!/usr/bin/make

configure: configure-stamp
configure-stamp:
	@dh_testdir
build: config.status
	@dh_testdir
	$(MAKE)
	touch $@

clean:
	@make clean
	@dh_clean

install: build
	@dh_testdir
	@dh_testroot

	@make install

.PHONY: clean install