#!/usr/bin/make

configure: configure-stamp
configure-stamp:
	dh_testdir
build: build-stamp
build-stamp:
	dh_testdir
	make

clean:
	make clean
	dh_clean

install: build
	dh_testdir
	dh_testroot

	make install